객체 배열
참조 자료형을 선언하느 객체 배열, 배열만 생성한 경우 null로 초기화
각 요소를 new로 지정해야한다.

..

01)
--Book
package chap07.array1;

public class Book {
    private String bookName;
    private String author;

    public Book() {} // 디폴트 생성자

    //책 이름과 저자 이름을 매개변수 받는 생성자
    public Book(String bookName, String author) {
        this.bookName = bookName;
        this.author = author;
    }

    public String getBookName() {
        return bookName;
    }

    public void setBookName(String bookName) {
        this.bookName = bookName;
    }

    public String getAuthor() {
        return author;
    }

    public void setAuthor(String author) {
        this.author = author;
    }

    public void showBookInfo() {
        System.out.println(bookName + ", " + author);
    }
}

--BookArray
package chap07.array1;

public class BookArray {
    public static void main(String[] args) {
        Book[] library = new Book[5];

        for (int i = 0; i < library.length; i++) {
            System.out.println(library[i]);
        }
    }
}
--출력결과
null
null
null
null
null

--BookArray1
package chap07.array1;

public class BookArray1 {
    public static void main(String[] args) {
        Book[] library = new Book[5];


            library[0] = new Book("태백산맥1", "조정래");
            library[1] = new Book("태백산맥2", "조정래");
            library[2] = new Book("태백산맥3", "조정래");
            library[3] = new Book("태백산맥4", "조정래");
            library[4] = new Book("태백산맥5", "조정래");

        for (int i = 0; i < library.length; i++) {
            library[i].showBookInfo();
        } // 지정된 값이 나옴

        for (int i = 0; i < library.length; i++) {
                System.out.println(library[i]);
        } // 주소값이 나옴
    }
}
--출력결과
태백산맥1, 조정래
태백산맥2, 조정래
태백산맥3, 조정래
태백산맥4, 조정래
태백산맥5, 조정래
chap07.array1.Book@506e1b77
chap07.array1.Book@4fca772d
chap07.array1.Book@9807454
chap07.array1.Book@3d494fbf
chap07.array1.Book@1ddc4ec2

..

배열 복사
기존 배열과 같은 배열을 만들거나 배열이 꽉찬 경우 더큰 배열을 만들고 기존 배열 자료를 복사할 수 있다. 

..

얕은 복사
배열 요소의 주소만 복사되므로 배열 요소가 변경되면 복사된 배열의 값도 변경됨

..

--ObjectCopy
package chap07.array1;

public class ObjectCopy1 {
    public static void main(String[] args) {

        Book[] bookArray1 = new Book[3];
        Book[] bookArray2 = new Book[3];

        bookArray1[0] = new Book("데미안1", "헤르만 헤세");
        bookArray1[1] = new Book("데미안2", "헤르만 헤세");
        bookArray1[2] = new Book("데미안3", "헤르만 헤세");

        System.arraycopy(bookArray1,0,bookArray2,0,3);



        for (int i = 0; i < bookArray2.length; i++) {
            bookArray1[i].showBookInfo();
            System.out.println("-----");
            bookArray2[i].showBookInfo();
        }
    }
}
--출력결과
데미안1, 헤르만 헤세
-----
데미안1, 헤르만 헤세
데미안2, 헤르만 헤세
-----
데미안2, 헤르만 헤세
데미안3, 헤르만 헤세
-----
데미안3, 헤르만 헤세

..

control+R은 코드에서 잘못쓴 오타 고치는 것

..

배열복사_얕은 복사_값변경

..
--ObjectCopy
package chap07.array1;

public class ObjectCopy2 {
    public static void main(String[] args) {

        Book[] bookArray1 = new Book[3];
        Book[] bookArray2 = new Book[3];

        bookArray1[0] = new Book("데미안1", "헤르만 헤세");
        bookArray1[1] = new Book("데미안2", "헤르만 헤세");
        bookArray1[2] = new Book("데미안3", "헤르만 헤세");

        System.arraycopy(bookArray1,0,bookArray2,0,3);

        for (int i = 0; i < bookArray2.length; i++) {
            bookArray2[i].showBookInfo();
        }

        bookArray1[0].setBookName("나목");
        bookArray1[0].setAuthor("박완서");

        System.out.println("-----bookArray1-----");
        for (int i = 0; i < bookArray1.length; i++) {
            bookArray1[i].showBookInfo();
        }


        System.out.println("-----bookArray2-----");
        for (int i = 0; i < bookArray2.length; i++) {
            bookArray2[i].showBookInfo();
        }
    }
}

..

깊은 복사

..
--ObjectCopy3
package chap07.array1;

public class ObjectCopy3 {
    public static void main(String[] args) {

        Book[] bookArray1 = new Book[3];
        Book[] bookArray2 = new Book[3];

        bookArray1[0] = new Book("데미안1", "헤르만 헤세");
        bookArray1[1] = new Book("데미안2", "헤르만 헤세");
        bookArray1[2] = new Book("데미안3", "헤르만 헤세");

        // 디폴트 생성자로 인스턴스 만듬
        bookArray2[0] = new Book();
        bookArray2[1] = new Book();
        bookArray2[2] = new Book();

        for (int i = 0; i < bookArray1.length; i++) {
            bookArray2[i].setBookName(bookArray1[i].getBookName());
            bookArray2[i].setAuthor(bookArray1[i].getAuthor());
        }
        /*System.arraycopy(bookArray1,0,bookArray2,0,3);*/ // 이거 대신에 위에로 바꾸면, bookArray1의 값이 달라져도, bookArray2의 값이 변하지 않는다.

        for (int i = 0; i < bookArray2.length; i++) {
            bookArray2[i].showBookInfo();
        }
        bookArray1[0].setBookName("나목");
        bookArray1[0].setAuthor("박완서");
        System.out.println("-----bookArray1-----");
        for (int i = 0; i < bookArray1.length; i++) {
            bookArray1[i].showBookInfo();
        }
        System.out.println("-----bookArray2-----");
        for (int i = 0; i < bookArray2.length; i++) {
            bookArray2[i].showBookInfo();
        }
    }
}
--출력결과
데미안1, 헤르만 헤세
데미안2, 헤르만 헤세
데미안3, 헤르만 헤세
-----bookArray1-----
나목, 박완서
데미안2, 헤르만 헤세
데미안3, 헤르만 헤세
-----bookArray2-----
데미안1, 헤르만 헤세
데미안2, 헤르만 헤세
데미안3, 헤르만 헤세


..

향상된 for문
배열 요소의 처음부터 끝까지 모든 요소를 참조할때 편리한 반복문

..

--EnhanceForLoop
package chap07.array1;

public class EnhanceForLoop {
    public static void main(String[] args) {
        String[] strArray = {"자바", "C", "Python", "JavaScript", "Android"};

        for (String lang : strArray) {
            System.out.println(lang);
        }

        int[] arr = {1, 2, 3, 4, 5};

        for (int i : arr) {
            System.out.println(i);
        }
    }
}
--출력결과
자바
C
Python
JavaScript
Android
1
2
3
4
5

..

arrayList 
기존 배열은 길이를 정하여 선언하므로, 사용중에 부족한 경우 다른 배열로 하는 배열리스트
요소를 추가하거나 제어 할때 각 내부에서 코드가 모두 구련되어 있어, 직접 선언하여 사용하는 것보다 편함

..

02)
package chap07.array1;

import java.util.ArrayList;

public class ArrayListTest {
    public static void main(String[] args) {
        // 선언
        ArrayList<Book> library = new ArrayList<>();
        // add() 메서드 요소값 추가
        library.add(new Book("태백산맥1", "조경래"));
        library.add(new Book("태백산맥2", "조경래"));
        library.add(new Book("태백산맥3", "조경래"));
        library.add(new Book("태백산맥4", "조경래"));

        for (int i = 0; i < library.size(); i++) { //arrayList에서는 size를 사용해야 오류가 안난다고 한다. length를 쓰고 싶으면 array를 만들어줘야함.
            Book book = library.get(i);
            book.showBookInfo();
        }
    }
}

--출력결과
태백산맥1, 조경래
태백산맥2, 조경래
태백산맥3, 조경래
태백산맥4, 조경래

..

03)
--Subject
package chap07.arraylist;

public class Subject {
    private String name;
    private int scorePoint;

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public int getScorePoint() {
        return scorePoint;
    }

    public void setScorePoint(int scorePoint) {
        this.scorePoint = scorePoint;
    }
}
--Student
package chap07.arraylist;

import java.util.ArrayList;

public class Student {
    int studnetID;
    String studentName;
    ArrayList<Subject> subjectList;

    public Student(int studnetID, String studentName) {
        this.studnetID = studnetID;
        this.studentName = studentName;
        subjectList = new ArrayList<>();
    }

    public void addSubject(String name, int score) {
        Subject subject = new Subject();
        subject.setName(name);
        subject.setScorePoint(score);
        subjectList.add(subject);
    }

    public void showStudentInfo() {
        int total = 0;

        for (Subject s : subjectList) {
            total += s.getScorePoint();
            System.out.println("학생" + studentName + "의" + s.getName() + "성적은 " + s.getScorePoint() + "점 입니다.");
        }
        System.out.println("학생"+studentName+"의 총점은 "+total+"입니다.");
    }

}
--StudentTest
package chap07.arraylist;

public class StudentTest {
    public static void main(String[] args) {

        Student studentLee = new Student(100, "이코사");
        studentLee.addSubject("국어", 100);
        studentLee.addSubject("수학", 99);
        Student studentKim = new Student(101, "김코사");
        studentKim.addSubject("국어", 100);
        studentKim.addSubject("수학", 99);
        studentKim.addSubject("영어", 99);

        studentLee.showStudentInfo();
        System.out.println("-----");
        studentKim.showStudentInfo();
    }
}
-- 출력결과
학생이코사의국어성적은 100점 입니다.
학생이코사의수학성적은 99점 입니다.
학생이코사의 총점은 199입니다.
-----
학생김코사의국어성적은 100점 입니다.
학생김코사의수학성적은 99점 입니다.
학생김코사의영어성적은 99점 입니다.
학생김코사의 총점은 298입니다.

..

문제1

..
--Book
package chap07.arraylist.book;

public class Book {
    private String title;
    private int price;

    public String getTitle() {
        return title;
    }

    public void setTitle(String title) {
        this.title = title;
    }

    public int getPrice() {
        return price;
    }

    public void setPrice(int price) {
        this.price = price;
    }
}
--Library
package chap07.arraylist.book;

import java.util.ArrayList;

public class Library {
    int bookPrice;
    String bookName;
    ArrayList<Book> booklist;

    public Library() {
        booklist = new ArrayList<>();
    }

    public void addBook(String title, int price) {
        Book book = new Book();
        book.setTitle(title);
        book.setPrice(price);
        booklist.add(book);
    }

    public void showBookInfo() {
        for (Book book : booklist) {
            System.out.println("책 제목 : " + book.getTitle() + ", 가격 : " + book.getPrice() + "원");
        }
    }
}
--LibraryTest
package chap07.arraylist.book;

public class LibraryTest {
    public static void main(String[] args) {
        Library library = new Library();
        library.addBook("자바 입문", 30000);
        library.addBook("스프링 부트", 35000);
        library.addBook("데이터베이스", 25000);

        library.showBookInfo();
    }

}
--출력결과
책 제목 : 자바 입문, 가격 : 30000원
책 제목 : 스프링 부트, 가격 : 35000원
책 제목 : 데이터베이스, 가격 : 25000원


..

상속
클래스를 정의할때 이미 구현된 클래스를 상속 받아서 속성이나 기능이 확장되는 클래스로 구현

..
--customer
package chap08.inheritance;

public class Customer {
    protected int customerID;
    protected String customerName;
    protected String customerGrade;
    int bonusPoint;
    double bonusRatio;

    public Customer() {
        customerGrade = "SILVER";
        bonusRatio = 0.01;

        System.out.println("Customer() 생성자 호출");
    }

    public int calcPrice(int price) {
        bonusPoint += price * bonusRatio;
        return price;
    }

    public String showCustomerInfo() {
        return customerName+"님의 등급은 " +customerGrade+"이며, 보너스 포인트는 "+bonusPoint+ "입니다.";
    }

    public String getCustomerName() {
        return customerName;
    }

    public void setCustomerName(String customerName) {
        this.customerName = customerName;
    }

    public int getCustomerID() {
        return customerID;
    }

    public void setCustomerID(int customerID) {
        this.customerID = customerID;
    }

    public String getCustomerGrade() {
        return customerGrade;
    }

    public void setCustomerGrade(String customerGrade) {
        this.customerGrade = customerGrade;
    }
}
--VIPcustomer
package chap08.inheritance;

public class VIPCustomer extends Customer {
    private int agentID;
    double salerRatio;

    // 디폴트 생성자
    public VIPCustomer() {
        customerGrade = "VIP";
        salerRatio = 0.05;
        bonusRatio = 0.1;
        System.out.println("VIPCustomer() 생성자 호출");
    }

    //보너스 포인트 생성자
    public int calcPrice(int price) {
        bonusPoint += price * bonusRatio;
        return price-(int)(price*salerRatio);
    }

    public int getAgentID() {
        return agentID;
    }

}
--customertest
package chap08.inheritance;

public class CustomerTest {
    public static void main(String[] args) {
        Customer customerLee = new Customer();
        customerLee.setCustomerID(1000);
        customerLee.setCustomerName("이순신");
        customerLee.bonusPoint = 10000;

        VIPCustomer customerKim = new VIPCustomer();
        customerKim.setCustomerID(1001);
        customerKim.setCustomerName("김유신");
        customerKim.bonusPoint = 10000;

        System.out.println(customerKim.showCustomerInfo());
        System.out.println(customerLee.showCustomerInfo());
    }
}
--출력결과
김유신님의 등급은 VIP이며, 보너스 포인트는 10000입니다.
이순신님의 등급은 SILVER이며, 보너스 포인트는 10000입니다.

..

묵시적 형변환[업케스팅]

--customertest2
package chap08.inheritance;

public class CustomerTest2 {
    public static void main(String[] args) {
        Customer customerLee = new Customer();
        customerLee.setCustomerID(1000);
        customerLee.setCustomerName("이순신");
        customerLee.bonusPoint = 10000;

        VIPCustomer customerKim = new VIPCustomer();
        customerKim.setCustomerID(1001);
        customerKim.setCustomerName("김유신");
        customerKim.bonusPoint = 10000;

        Customer vc = new VIPCustomer();
        vc.setCustomerID(1002);
        vc.setCustomerName("김선희");
        vc.bonusPoint = 10000;

        System.out.println(vc.showCustomerInfo());

        System.out.println(customerKim.showCustomerInfo());
        System.out.println(customerLee.showCustomerInfo());
    }
}
--출력결과
김선희님의 등급은 VIP이며, 보너스 포인트는 10000입니다.
김유신님의 등급은 VIP이며, 보너스 포인트는 10000입니다.
이순신님의 등급은 SILVER이며, 보너스 포인트는 10000입니다.

--customertest3
package chap08.inheritance;

public class CustomerTest {
    public static void main(String[] args) {
        Customer customerLee = new Customer();
        customerLee.setCustomerID(1000);
        customerLee.setCustomerName("이순신");
        customerLee.bonusPoint = 10000;

        VIPCustomer customerKim = new VIPCustomer();
        customerKim.setCustomerID(1001);
        customerKim.setCustomerName("김유신");
        customerKim.bonusPoint = 10000;

        System.out.println(customerKim.showCustomerInfo());
        System.out.println(customerLee.showCustomerInfo());
    }
}
--출력결과
김유신님의 등급은 VIP이며, 보너스 포인트는 10000입니다.
이순신님의 등급은 null이며, 보너스 포인트는 10000입니다.
.. 

메서드 오버라이딩
상위 클래스에서 정의된 메서드 중 하위클래스와 기능이 맞지 않거나 추가 기능이 필요한 경우 같은 이름과 매개 변수로 하위 클래스에서 재정의

..
--customer
package chap08.inheritance;

public class Customer {
    protected int customerID;
    protected String customerName;
    protected String customerGrade;
    int bonusPoint;
    double bonusRatio;

    public Customer(){
        customerGrade = "SILVER";
        bonusRatio = 0.01;
    }

    public Customer(String customerName,int customerID){
        this.customerID = customerID;
        this.customerName = customerName;
        customerGrade = "SILVER";
        bonusRatio = 0.01;

        //System.out.println("Customer() 생성자 호출");
    }

    // 보너스 포인트 계산
    public int calcPrice(int price) {
        bonusPoint += (price * bonusRatio);
        return price;
    }

    public String showCustomerInfo() {
        return customerName+"님의 등급은 " +customerGrade+"이며, 보너스 포인트는 "+bonusPoint+ "입니다.";
    }

    public String getCustomerName() {
        return customerName;
    }

    public void setCustomerName(String customerName) {
        this.customerName = customerName;
    }

    public int getCustomerID() {
        return customerID;
    }

    public void setCustomerID(int customerID) {
        this.customerID = customerID;
    }

    public String getCustomerGrade() {
        return customerGrade;
    }

    public void setCustomerGrade(String customerGrade) {
        this.customerGrade = customerGrade;
    }
}
--VIPCustomer
package chap08.inheritance;

public class VIPCustomer extends Customer {
    private int agentID;
    double salerRatio;

    // 디폴트 생성자
    public VIPCustomer() {
        customerGrade = "VIP";
        salerRatio = 0.05;
        bonusRatio = 0.1;
       // System.out.println("VIPCustomer() 생성자 호출");
    }

    public VIPCustomer(String customerName, int customerID, int agentID) {
        super(customerName, customerID);
        customerGrade = "VIP";
        salerRatio = 0.05;
        bonusRatio = 0.1;
        this.agentID = agentID;
    }

    //VIP보너스 포인트 생성자
    public int calcPrice(int price) {
        bonusPoint += (price * bonusRatio);
        return price-(int)(price*salerRatio);
    }

    public int getAgentID() {
        return agentID;
    }

}

--Overrideing1

package chap08.inheritance;

public class Overriding {
    public static void main(String[] args) {
        Customer customerLee = new Customer(1010, "이순신");
        customerLee.bonusPoint = 1000;

        VIPCustomer customerKim = new VIPCustomer(1020, "김유신",0000);
        customerKim.bonusPoint = 1000;

        int price = 100000;
        System.out.println(customerLee.getCustomerName()+"님이 지불해야 하는 금액은 "+customerLee.calcPrice(price)+"원입니다.");
        System.out.println(customerKim.getCustomerName()+"님이 지불해야 하는 금액은 "+customerKim.calcPrice(price)+"원입니다.");
    }
}
--출력결과
이순신님이 지불해야 하는 금액은 100000원입니다.
김유신님이 지불해야 하는 금액은 95000원입니다.


--Overrideing2

package chap08.inheritance;

public class Overriding1 {
    public static void main(String[] args) {
        Customer vc = new VIPCustomer(1010, "이순신",1111);
        vc.bonusPoint = 1000;

        int price = 100000;
        System.out.println(vc.getCustomerName()+"님이 지불해야 하는 금액은 "+vc.calcPrice(price)+"원입니다.");
        System.out.println();
    }
}
--출력결과
이순신님이 지불해야 하는 금액은 95000원입니다.

--Overrideing3

package chap08.inheritance;

public class Overridin3 {
    public static void main(String[] args) {
        int price = 100000;
        Customer vc = new VIPCustomer(1010, "이순신",1111);

        Customer customerLee = new Customer(1010, "이순신");
        customerLee.bonusPoint = 1000;

        VIPCustomer customerKim = new VIPCustomer(1020, "김유신",0000);
        customerKim.bonusPoint = 1000;

        System.out.println(customerLee.getCustomerName()+"님이 지불해야 하는 금액은 "+customerLee.calcPrice(price)+"원입니다.");
        System.out.println(customerKim.getCustomerName()+"님이 지불해야 하는 금액은 "+customerKim.calcPrice(price)+"원입니다.");
        System.out.println(vc.getCustomerName()+"님이 지불해야 하는 금액은 "+vc.calcPrice(price)+"원입니다.");
    }
}
--출력결과
이순신님이 지불해야 하는 금액은 100000원입니다.
김유신님이 지불해야 하는 금액은 95000원입니다.
이순신님이 지불해야 하는 금액은 95000원입니다.

..

문제2

..
--GoldCustomer
package chap08.inheritance;

public class GoldCustomer extends Customer {
    double salerRatio;

    public GoldCustomer() {
        customerGrade = "GOLD";
        salerRatio = 0.05;
        bonusRatio = 0.02;
    }

    public GoldCustomer (int customerID, String customerName) {
        super(customerName, customerID);
        customerGrade = "GOLD";
        salerRatio = 0.05;
        bonusRatio = 0.02;
    }

    public int calcPrice(int price) {
        bonusPoint += (price * bonusRatio);
        return price-(int)(price*salerRatio);
    }
}

--GoldCustomerTest
package chap08.inheritance;

public class GoldTest {
    public static void main(String[] args) {
        Customer customer1 = new GoldCustomer(10030, "강감찬");
        int price = 20000;

        System.out.println(customer1.getCustomerName()+" 님이 지불해야 하는 금액은 "+customer1.calcPrice(price)+"원입니다.");
    }

}
--출력결과
강감찬 님이 지불해야 하는 금액은 19000원입니다.


--PlatinumCustomer
package chap08.inheritance;

public class PlatinumCustomer extends Customer {
    double salerRation;
    int agentID;

    public PlatinumCustomer(){
        customerGrade = "PLATINUM";
        salerRation = 0.12;
        bonusRatio = 0.07;
    }

    public PlatinumCustomer(int customerID, String customerName,int agentID) {
        super(customerName, customerID);
        customerGrade = "PLATINUM";
        salerRation = 0.15;
        bonusRatio = 0.07;
        this.agentID= agentID;
    }

    public int calcPrice(int price) {
        bonusPoint += price * bonusRatio;
        return price - (int) (price * salerRation);
    }

}

--PlatinumCustomerTest
package chap08.inheritance;

public class PlatinumTest {
    public static void main(String[] args) {
        int price = 40000;

        Customer customerU = new PlatinumCustomer(10040, "유관순",3000);

        System.out.println(customerU.getCustomerName()+" 님이 지불해야 하는 금액은 "+customerU.calcPrice(price)+"원입니다.");

    }
}
--출력결과
유관순 님이 지불해야 하는 금액은 34000원입니다.

